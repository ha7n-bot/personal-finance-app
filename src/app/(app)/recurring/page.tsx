import { db } from "@/lib/db";import { formatSAR } from "@/lib/money";import { requireUserId } from "@/lib/current-user";import { createRecurring } from "../financial-actions";
export default async function RecurringPage(){const userId=await requireUserId();const [rows,accounts]=await Promise.all([db.recurringPayment.findMany({where:{userId,active:true},orderBy:{nextDueAt:"asc"}}),db.account.findMany({where:{userId,isArchived:false}})]);return <div><h1 className="text-3xl font-bold mb-8">الالتزامات القادمة</h1><div className="grid lg:grid-cols-[1fr_360px] gap-5"><section className="space-y-3">{rows.map(row=><article className="card p-5 flex justify-between" key={row.id}><div><strong>{row.name}</strong><p className="muted mt-1">الاستحقاق {row.nextDueAt.toLocaleDateString("ar-SA")}</p></div><strong>{formatSAR(row.amount)}</strong></article>)}{!rows.length&&<div className="card p-8 muted">لا توجد التزامات مسجلة.</div>}</section><form action={createRecurring} className="card p-6 space-y-4"><h2 className="text-xl font-bold">التزام جديد</h2><input className="field" name="name" placeholder="الاسم" required/><input className="field" name="amount" type="number" min="1" step="0.01" placeholder="المبلغ" required/><input className="field" name="nextDueAt" type="date" required/><select className="field" name="frequency"><option value="MONTHLY">شهري</option><option value="WEEKLY">أسبوعي</option><option value="QUARTERLY">ربع سنوي</option><option value="YEARLY">سنوي</option></select><select className="field" name="accountId"><option value="">بدون حساب</option>{accounts.map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</select><button className="btn w-full">إضافة</button></form></div></div>}
