import { Prisma } from "@prisma/client";import { db } from "@/lib/db";import { formatSAR } from "@/lib/money";import { requireUserId } from "@/lib/current-user";import { createDebt } from "../financial-actions";
export default async function DebtsPage(){const userId=await requireUserId();const debts=await db.debt.findMany({where:{userId},include:{payments:true},orderBy:{createdAt:"desc"}});return <div><h1 className="text-3xl font-bold mb-8">الديون والالتزامات</h1><div className="grid lg:grid-cols-[1fr_360px] gap-5"><section className="space-y-4">{debts.map(debt=>{const paid=debt.payments.reduce((s,p)=>s.plus(p.amount),new Prisma.Decimal(0));const percent=Math.min(100,paid.div(debt.originalAmount).mul(100).toNumber());return <article className="card p-6" key={debt.id}><div className="flex justify-between"><h2 className="font-bold text-xl">{debt.name}</h2><span>{percent.toFixed(1)}%</span></div><div className="grid grid-cols-3 gap-3 mt-5 text-sm"><div><p className="muted">الإجمالي</p><strong>{formatSAR(debt.originalAmount)}</strong></div><div><p className="muted">المدفوع</p><strong>{formatSAR(paid)}</strong></div><div><p className="muted">المتبقي</p><strong>{formatSAR(debt.originalAmount.minus(paid))}</strong></div></div><div className="h-2 bg-black/10 rounded-full mt-5"><div className="h-full bg-emerald-500 rounded-full" style={{width:`${percent}%`}}/></div></article>})}{!debts.length&&<div className="card p-8 muted">لا توجد ديون مسجلة حاليًا.</div>}</section><form action={createDebt} className="card p-6 space-y-4"><h2 className="text-xl font-bold">إضافة دين</h2><input className="field" name="name" placeholder="اسم الدين" required/><input className="field" name="originalAmount" type="number" min="1" step="0.01" placeholder="إجمالي الدين" required/><input className="field" name="installmentAmount" type="number" min="0" step="0.01" placeholder="القسط"/><input className="field" name="nextPaymentAt" type="date"/><button className="btn w-full">إضافة</button></form></div></div>}
